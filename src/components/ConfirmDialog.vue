<template>
<v-dialog v-model="isDialog" max-width="500px">
  <v-card>
    <v-card-title class="text-h5">Are you sure you want to delete this item?</v-card-title>
    <v-card-actions>
      <v-spacer></v-spacer>
      <v-btn color="blue-darken-1" variant="text" @click="data=false">Cancel</v-btn>
      <v-btn color="blue-darken-1" variant="text" @click="data=true">OK</v-btn>
      <v-spacer></v-spacer>
    </v-card-actions>
  </v-card>
</v-dialog>
</template>
<script setup>
import { ref } from 'vue'

const isDialog = ref(false)
const data = ref(null)

const deleteItemConfirm = () => {
    console.log('deleteItemConfirm')
    // Do delete operation
    isDialog.value = false

    data.value = true

    return data.value
  }

defineExpose({
    isDialog,
  deleteItemConfirm,
})
</script>